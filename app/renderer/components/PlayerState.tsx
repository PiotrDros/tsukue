interface PlayerState {
    playing: boolean
    paused: boolean
    stopped: boolean
    shuffled: boolean
    repeating: boolean
    repeatingOne: boolean
    muted: boolean
}

export default PlayerState
